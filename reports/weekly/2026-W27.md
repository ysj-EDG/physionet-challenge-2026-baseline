# 第 27 周工作汇报

## 本周完成

- 完成 LSTM 主路线的阈值策略和类别不平衡权重调整，更新训练后的 `pos_weight` 配置，并重新导出 Kaggle 版本模型与推理结果。
- 调整后的模型使用 Platt logistic calibration，二分类阈值更新为 `0.078162`；该阈值与 Youden 最优阈值接近，test 集最优阈值为 `0.079217`，external 集最优阈值为 `0.076202`。
- 更新后的 test 集结果：AUROC `0.770563`，AUPRC `0.239689`，Accuracy `0.727848`，F1 `0.295082`，Precision `0.180000`，Recall `0.818182`，Specificity `0.721088`，混淆矩阵为 TP=9、TN=106、FP=41、FN=2。
- 更新后的 external 结果：AUROC `0.665761`，AUPRC `0.269462`，Accuracy `0.666667`，F1 `0.307692`，Precision `0.222222`，Recall `0.500000`，Specificity `0.695652`，混淆矩阵为 TP=4、TN=32、FP=14、FN=4。
- 将更新后的 Kaggle 模型和推理结果补充归档到 `reports/milestones/output/lstm_model_kaggle/` 和 `reports/milestones/output/lstm_results_kaggle/`，来源分别为 `lstm_model_kaggle` 与 `lstm_results_kaggle`。

## 当前问题

- 当前阈值调整明显提高了 test 集召回率，但 precision 仍偏低，假阳性数量较多，后续需要继续在召回和误报之间做权衡。
- external 集 AUROC 和 Recall 仍低于 test 集，说明模型泛化仍不稳定，需要继续检查数据分布差异和特征鲁棒性。
- NeuroCardioKit 中 EEG 相关的 BSR 等特征还没有完成融合；其他部分已添加并融合。
- HRV 中部分 30s 级特征还未整理完成，目前只将 11 维/epoch 的 HRV 特征融合进 LSTM。
- `multi_extract_from_mid.py` 还没有加入当前主路线。

## 下周计划

- 根据本轮 Kaggle 版本结果继续评估阈值策略，重点关注 false positive 控制和 external 泛化表现。
- 继续整理并融合 NeuroCardioKit 的 EEG BSR 等剩余特征。
- 梳理 HRV 30s 特征，评估是否替换或扩展当前 11 维/epoch 表示。
- 评估特征数量、`pos_weight` 设置和校准方式，准备下一版官方测试提交。
