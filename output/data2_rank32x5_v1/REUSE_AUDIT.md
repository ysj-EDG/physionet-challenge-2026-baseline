# Reuse audit

- Compact30 definition and feature order: `pooled_logistic_v2.COMPACT_INDICES` plus the first ten demographic columns, exposed through `run_p3_v2.feature_names(..., P3_compact30)` and asserted against the frozen 30-name contract.
- Frozen matrices: `feat_input2/output/data2_lowcost/cache/data2_patient_summaries.npz`, generated by `run_data2_lowcost.py`; each outer fold uses its corresponding `p3_<holdout>_compact30` matrix.
- Fold-local preprocessing: reused `run_p3_v2.fit_imputer_scaler` and `apply_imputer_scaler`, fit only on the two outer training sites.
- Official metrics: reused `run_loso_1103.metric_bundle` and `evaluate_model.compute_auroc_age` (gap=2).
- LOSO membership: reused the frozen `data2_manifest.csv` site labels and counts.
- New logic: only deterministic age-windowed pair subsampling, symmetric pair construction, equal site-total sample weighting, five raw-score ranker ensemble, diagnostics, and report writing.
- No second compact30 definition, feature scaler, AC metric, or NPZ was created.
